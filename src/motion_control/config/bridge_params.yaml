/**:
  ros__parameters:
    # Canonical Ackermann command:
    #   linear.x  = longitudinal speed (m/s)
    #   angular.z = front-wheel steering angle (rad)
    command_topic: "/ackermann_cmd"

    # --- UDP communication ---
    udp_host: "192.168.1.50"
    udp_port: 5000
    bind_device: "enp5s0"
    bind_interface: "192.168.1.11"

    # Vehicle geometry and motion limits are intentionally not centralized in
    # this branch yet. Values not specified here fall back to bridge_node
    # defaults and may be overridden later once the real-vehicle parameter
    # strategy is finalized.

    # --- Actuator enable mask ---
    # bit0: RT49 (drive), bit1: EPS (steering), bit2: SEB (brake)
    enable_mask: 7

    # --- Control timing ---
    publish_rate: 20.0
    # Seconds without /ackermann_cmd before sending zero-velocity safety frames.
    command_timeout: 0.5
