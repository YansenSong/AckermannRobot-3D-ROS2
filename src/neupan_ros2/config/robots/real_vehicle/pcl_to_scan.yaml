/**:
  ros__parameters:
    use_sim_time: false
    # Frame, height, angle, timing, and range parameters are injected from
    # vehicle_config/config/real_vehicle.yaml at launch time.
    concurrency_level: 1
